@model CouponRequest

<div class="card shadow border-0 mt-4">
    <div class="card-header bg-warning text-white">
        <h1 class="mb-0">Edit Coupon</h1>
    </div>
    <div class="card-body">
        <form asp-action="Edit" method="post">
            <div class="mb-3">
                <label asp-for="Code" class="form-label">Coupon Code</label>
                <input asp-for="Code" class="form-control" placeholder="Enter coupon code" />
                <span asp-validation-for="Code" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <label asp-for="DiscountAmount" class="form-label">Discount Amount</label>
                <input asp-for="DiscountAmount" class="form-control" type="number" step="0.01" placeholder="Enter discount amount" />
                <span asp-validation-for="DiscountAmount" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <label asp-for="MinAmount" class="form-label">Minimum Amount</label>
                <input asp-for="MinAmount" class="form-control" type="number" placeholder="Enter minimum amount" />
                <span asp-validation-for="MinAmount" class="text-danger"></span>
            </div>
            <div class="text-end">
                <button type="submit" class="btn btn-success">Save Changes</button>
                <a asp-action="Index" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
